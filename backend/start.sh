#!/bin/bash

# Script de démarrage pour le backend Django GAM
# Exécuté automatiquement par Docker/Railway au déploiement

set -e  # Arrêter en cas d'erreur

echo "🚀 Starting GAM Backend..."

# ──────────────────────────────────────────────────────────────
# 1. Migrations de la base de données
# ──────────────────────────────────────────────────────────────
echo "📊 Applying database migrations..."
python manage.py migrate --noinput

# ──────────────────────────────────────────────────────────────
# 2. Collection des fichiers statiques
# ──────────────────────────────────────────────────────────────
echo "📦 Collecting static files..."
python manage.py collectstatic --noinput

# ──────────────────────────────────────────────────────────────
# 3. Migration des médias vers Supabase
# ──────────────────────────────────────────────────────────────
echo "☁️ Migrating media from Cloudinary to Supabase..."
python manage.py migrate_media_to_supabase

# ──────────────────────────────────────────────────────────────
# 4. Génération des vues pour les articles (si pas déjà fait)
# ──────────────────────────────────────────────────────────────
echo "📈 Generating article views..."
python manage.py shell < scripts/generate_views.py || echo "⚠️ Views generation skipped (may already exist)"

# ──────────────────────────────────────────────────────────────
# 5. Population des publicités (si pas déjà fait)
# ──────────────────────────────────────────────────────────────
echo "💰 Populating advertisements..."
python manage.py shell < scripts/populate_ads.py || echo "⚠️ Ads population skipped (may already exist)"

# ──────────────────────────────────────────────────────────────
# 6. Démarrage du serveur Gunicorn
# ──────────────────────────────────────────────────────────────
echo "✅ Initialization complete!"
echo "🌐 Starting Gunicorn server..."

exec gunicorn config.wsgi:application \
    --bind 0.0.0.0:8000 \
    --workers 4 \
    --threads 2 \
    --worker-class gthread \
    --timeout 120 \
    --access-logfile - \
    --error-logfile - \
    --log-level info
